/* Basic Reset and Variables */
:root {
    --primary-color: #3f51b5; /* A strong blue for branding */
    --accent-color: #ff9800; /* A bright orange for CTAs */
    --text-color: #333;
    --background-light: #f4f4f4;
}

body {
    font-family: sans-serif;
    margin: 0;
    padding: 0;
    color: var(--text-color);
    line-height: 1.6;
}

/* Header/Navigation */
header {
    background: var(--primary-color);
    color: white;
    padding: 15px 5%;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.5em;
    font-weight: bold;
}

nav a {
    color: white;
    text-decoration: none;
    margin: 0 15px;
}

.phone {
    font-weight: bold;
    font-size: 1.1em;
}

/* Hero Section */
.hero {
    background: var(--background-light);
    padding: 100px 5%;
    text-align: center;
}

.hero h1 {
    font-size: 2.5em;
    margin-bottom: 10px;
    color: var(--primary-color);
}

.hero p {
    font-size: 1.2em;
    margin-bottom: 30px;
}

/* Call to Action Buttons */
.cta-primary, .cta-secondary {
    display: inline-block;
    padding: 12px 30px;
    margin: 10px;
    text-decoration: none;
    border-radius: 5px;
    font-weight: bold;
    transition: background-color 0.3s;
}

.cta-primary {
    background-color: var(--accent-color);
    color: white;
}

.cta-secondary {
    background-color: transparent;
    color: var(--primary-color);
    border: 2px solid var(--primary-color);
}

/* Trust Bar */
.trust-bar {
    background: #e0e0e0;
    padding: 15px 5%;
    display: flex;
    justify-content: space-around;
    text-align: center;
    font-weight: bold;
}

/* Services Overview */
.services-overview {
    padding: 50px 5%;
    text-align: center;
}

.service-cards {
    display: flex;
    justify-content: space-around;
    gap: 20px;
    margin-top: 30px;
}

.card {
    background: white;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    flex-basis: 30%; /* Take up about a third of the space */
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
}

/* Footer */
footer {
    background: var(--text-color);
    color: white;
    text-align: center;
    padding: 20px;
    margin-top: 40px;
    font-size: 0.9em;
}